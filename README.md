# ChatGPT Style Gemini Chatbot

1. Create a .env file from .env.example
2. Add your Gemini API key.
3. Install dependencies:
   pip install -r requirements.txt
4. Run:
   python app.py

Deploy on Render using the same files.
